# === LiteLLM Gateway ===
LITELLM_MASTER_KEY=sk-change-me-to-a-secure-key

# === Local LLM (Ollama or compatible, referenced by gateway.yaml) ===
LOCAL_LLM_API_BASE=http://host.docker.internal:11434

# === OpenAI ===
OPENAI_API_KEY=

# === Anthropic ===
ANTHROPIC_API_KEY=

# === Database (PostgreSQL for usage tracking) ===
POSTGRES_USER=bsgateway
POSTGRES_PASSWORD=change-me-secure-password
POSTGRES_DB=bsgateway
COLLECTOR_DATABASE_URL=postgresql://bsgateway:change-me-secure-password@postgres:5432/bsgateway

# === Redis ===
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=

# === BSGateway API ===
JWT_SECRET=dev-jwt-secret-change-in-production
ENCRYPTION_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
SUPERADMIN_KEY=superadmin-dev-key

# === Development Seed Data ===
# Set to true to auto-create a test tenant + API key on startup
SEED_DEV_DATA=true
# Seed API key (use this for testing):
#   bsg_dev-test-key-do-not-use-in-production-000
# Seed tenant slug: dev-team
# Seed models: gpt-4o-mini, gpt-4o, claude-sonnet

# === Frontend (Vite Dev Server) ===
VITE_ALLOWED_HOSTS=bsserver
